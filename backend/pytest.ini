[pytest]
asyncio_mode = auto
testpaths = tests/unit tests/integration/section_lifecycle tests/integration/qa_chaos tests/load
markers =
    load: Load test (may be slow, uses concurrent execution)
    slow: Slow test expected to take more than a few seconds
