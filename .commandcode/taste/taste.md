# Taste
See [taste/taste.md](taste/taste.md)

- Expects the agent to perform production database repairs over SSH directly, writing SQL to a local temp file, `scp`-ing it to the server, and piping it into `psql -f /dev/stdin` inside the DB container — because PowerShell mangles inline SQL/quotes and does not support `<` input redirection, the temp-file-plus-scp pattern is the established way to run multi-statement SQL (including `BEGIN;...COMMIT;` transactions) on the production Postgres. Confidence: 0.7

- Only commits and pushes fixes once sure the work is complete; ignores unrelated changes when committing fixes — when told just "push", committed only the relevant login-page change and left unrelated tooling state (e.g., `.commandcode` files) uncommitted. Repeated with a larger change set: a bare "push" triggered committing the 11-file unified-login rework and pushing to main, again leaving `.commandcode/` state uncommitted. A third bare "push" again committed only the linked-students fix (`backend/app/modules/portal_internal/service.py`) and pushed to main with `.commandcode/` still left uncommitted. A fourth terse "yes pus" (push) after the ERP-origin auto-detection fix again produced a commit scoped to just the five portal frontend files and a push to main, with `.commandcode/` still left uncommitted. A fifth bare "push" after the mobile-first portal Phase 1 rework again produced a commit scoped to exactly the `portal/frontend` changes (14 files, `feat(portal):` subject with detailed body) and a push to main, with `.commandcode/` still left uncommitted. A sixth bare "push" after the bottom-nav notch fix again produced a commit scoped to exactly the two changed files (MobileTabBar.tsx + globals.css) and a push to main, with `.commandcode/` still left uncommitted. A seventh bare "push" after the notch-mask correction again produced a commit scoped to exactly the two changed files (globals.css + MobileTabBar.tsx) and a push to main, with `.commandcode/` still left uncommitted. An eighth bare "push" after the personalized-header work again produced a commit scoped to exactly the three changed files (DashboardHeader.tsx, OverflowSheet.tsx, layout.tsx) and a push to main, with `.commandcode/` still left uncommitted. A ninth bare "commit and push" after the marketing-app extraction again produced a commit scoped to exactly the relevant change set (the new `marketing/` app plus the related ERP-frontend and backend CORS edits, 27 files) and a push to main, with `.commandcode/` and `graphify-out/` left uncommitted. A tenth bare "push" after the monorepo refactor (apps/ grouping + templates relocation) again produced a commit scoped to exactly the refactor set (640 files, all moves detected as renames, commit `bd63936`) and a push to main, with `.commandcode/` and `graphify-out/` left uncommitted. An eleventh bare "push" (with an unrelated IDE-context file open) after the CORS-origins fix again produced a commit scoped to exactly the four edited backend files (`fix(cors):` subject with detailed body, commit `e29ab3c`) and a push to main, with `.commandcode/` left uncommitted. A twelfth terse directive "merge and ppush" after the LessonForge feature produced the fuller merge workflow: a commit scoped to exactly the feature set (25 files, `feat(lessonforge):` subject with detailed body), a push of the feature branch, a fast-forward merge into main, and a push of main — with `.commandcode/`, `tsconfig.tsbuildinfo`, and the spec doc left uncommitted as usual. A thirteenth terse directive "continue and push" after the same feature's Phase 1+2 was already complete and verified was interpreted NOT as a green-light to expand into the plan's explicitly out-of-scope / "post-MVP" Phase 3 tuning (sticker caching/registry, content-safety retry, manual-regeneration UI), but as an instruction to commit exactly the completed Phase 1+2 change set (12 files, `feat(lessonforge):` detailed body, required Co-authored-by trailer) and push to main — including restoring the incidental `tsconfig.tsbuildinfo` build-cache regression to HEAD before committing so the commit stays scoped to real code, and leaving `.commandcode/` state uncommitted. Confidence: 0.99
- A push directive phrased as "push ur changes" means commit ONLY the agent's own changes — when the user's concurrent in-flight work is intermixed with the agent's edits inside the SAME file, expects hunk-level staging (compute the diff, filter to the agent's own hunks with recomputed headers, `git apply --cached --check` then `git apply --cached`, then `git add` the rest of the change set) so the user's unrelated hunks stay untouched in the working tree, verified before/after (`MM` on the shared file, staged-diff inspection, compiling the staged blobs, headless commit message via `git commit -F <file>` with the `Co-authored-by` trailer, then push and confirm the tree is in sync). Recurred when the working tree held a large uncommitted permissions feature plus unrelated edits to `academic/router.py` (the file open in the IDE when the directive was issued): the agent first proved the three shared pages contained only its own hunks (filtering `git diff -U0` output against the symbols it had touched), then staged exactly its own four files (`git add -- <three pages> <new test>`), printed staged-vs-unstaged file lists, restored the incidental `tsconfig.tsbuildinfo` to HEAD, committed with a scoped conventional-commit message, pushed main, and confirmed `origin/main` in sync — leaving every unrelated file (permissions work, `academic/router.py`, docs, `.commandcode/`) untouched in the working tree and explicitly reporting what was left alone. Confidence: 0.68
- Refers to commits by their subject line when requesting an undo (e.g., "undo this fix(portal): show linked students for student accounts, not just parents") and expects the agent to map the title to the commit and revert it — for already-pushed work that means creating a revert commit (checking intervening commits for conflicts first) and pushing, rather than rewriting history. Confidence: 0.6
- Rejects completed UI/design work outright on aesthetic grounds with a terse directive (e.g., "i did not like the idea , undo it") — even after the feature was fully implemented, verified, and reported — and expects an immediate surgical undo that removes only the rejected feature (its component files, config keyframes, and page wiring, restoring the original strings/behavior) while preserving unrelated fixes bundled in the same commit (e.g., the auth form-POST fix), followed by a clean build verification — not a defensive pitch to keep or adjust it. Confidence: 0.85
- Accepts (and repeatedly pushes without objection) a conventional-commit message format for fixes: `type(scope):` imperative subject plus a detailed body that explains the root cause, lists the per-file changes as bullets, calls out what was deliberately left alone, states the verification performed (typecheck/lint/test run, negative control), and ends with the `Co-authored-by: CommandCodeBot <noreply@commandcode.ai>` trailer — written to a temp file and passed via `git commit -F` because heredocs fail on this Windows shell. Confidence: 0.6
- Expects the git working tree to be verified correct and clean before a push — when tracked root files vanished mid-session (later restored from HEAD), asserted "i never deleted a file, you might did so so make sure that the git working tree is correct and clean"; expects the agent to take ownership of any file loss caused by its own tooling rather than deflect, prove integrity with `git ls-files --deleted` and a diff vs HEAD (nothing tracked missing, only intended changes), restore anything gone, and never let its operations delete or depend on gitignored root files. Extended to derived build artifacts: after a typecheck touched `tsconfig.tsbuildinfo`, the agent restored it to HEAD before committing so the push stays scoped to real code (".commandcode/" session state likewise never committed). Confidence: 0.62
- A bare push can carry a live server-verification step — "push and login to server to see if everything is working" means commit+push to main AND then SSH into the deployment server to confirm the running system actually works; when login is impossible (missing SSH key), expects a definitive report of the exact blocker and what the user must restore (the `.pem` from AWS) rather than a silently truncated task. Confidence: 0.55
- After the agent asks whether to run the proposed server-side remediation steps, a bare "yes" is full authority to execute the entire production fix autonomously (no further confirmation per step) — restart the unhealthy gateway/service, verify it becomes healthy, recover the stuck queue job (XACK the orphaned pending stream message, then re-publish it as a fresh stream entry so the healthy worker reprocesses it), and prove completion end-to-end live (worker log shows processing → HTTP 200 → completed; the result key is written; the pending list drains to 0); expects a root-cause report plus the steps taken, flagging residual cosmetic issues (e.g., a container image lacking `curl`, so its own healthcheck always reads "unhealthy" as a red herring) and optional hardening gaps (a single-attempt worker with no XAUTOCLAIM/retry will strand a future job the same way) rather than stopping at the first successful step. Recurred when a Vercel 502 (`ROUTER_EXTERNAL_TARGET_CONNECTION_ERROR`) outage was traced to a crash-looping backend — the production DB was stamped at migration `202608310001`, whose file a feature revert had deleted from `main`, so the entrypoint's `alembic upgrade head` failed on every restart — and one structured question with a recommended repair sufficed for approval to execute the whole production fix autonomously: back up the orphaned table rows, drop the table + restamp `alembic_version` to the code's head, recreate the backend container, start the Caddy proxy, verify end-to-end through the exact failing path (health 200 via the public IP and via the Vercel domain, login POST no longer 502), and report residual items honestly (Caddy ACME/TLS noise for non-resolving hostnames, a pre-existing app-level 500 on malformed login payloads distinct from the gateway error). Confidence: 0.72
- When pulling a feature back out of `main`, prefers a non-destructive freeze-first workflow: snapshot the feature's exact current state onto a dedicated freeze branch (named after the feature, e.g., `lessonforge-freeze`, created at the current main tip) and then revert `main` back to the pre-feature baseline with a single revert commit — preserving the full feature recoverably (the agent verifies the reverted tree is byte-identical to the pre-feature commit and pushes the freeze branch before main) rather than resetting history or losing the work. Confidence: 0.65

- Wants recurring operational needs encoded as a reusable, committed project skill/playbook he can invoke on demand, rather than one-off ad-hoc scripts — explicitly asked for "a skill that i will run in production everytime i want to make sure every damn thing is working". Confidence: 0.8

- Expects a production health check to be exhaustive and to produce a full, detailed report — "scan for all possible issues", "without missing a simpl small tiny thing", including scanning all the logs — and to know how to SSH to the server and cover the end-to-end public path, not just a subset. Confidence: 0.8

- Expects production diagnostics that will run against live data to be non-destructive/read-only — the health check must be safe to run "even when we launch the project and we have real data": it never restarts, writes, or mutates anything on the server, instead reporting findings and proposing the exact fix for the user to approve. Confidence: 0.6

- Prioritizes safety/least-privilege over feature completeness in architecture decisions and will reject a fully-planned, approved-looking feature outright on risk grounds — killed a UI-triggered full production health scan ("i will not implement it, i think it is risky") because it needed a web→host execution path (a systemd agent reading backend trigger files, or mounting `/var/run/docker.sock`), which would hand the Vercel-reachable web app root-equivalent control of the host; redirected to "improve the health page to include other metrics like containers health, redis, and others... everything we can make it a metrics will be better and safer." Prefers read-only, in-container metrics that add no new privilege, host access, subprocess, route, or permission — accepting reduced coverage (literal container state/restarts/OOM, host load/logs explicitly out of scope) in exchange for a smaller blast radius, and expecting the design to reuse the existing gated endpoint rather than create new surface. Confidence: 0.8

- Prefers to extend/enrich existing pages and surfaces with additional data (metrics) rather than standing up new subsystems or privileged components — when offered a host ops-agent, a Docker-socket mount, or in-container-only checks, chose the in-container option ("zero infra change, no host component at all") and framed the whole improvement as enriching the existing System Health page. Confidence: 0.6

- Reviews new production plumbing for the hosting layer's hard limits and per-connection resource cost before letting it ship — flagged that Vercel terminates proxied long-lived responses at a fixed execution limit (so a stream lifetime must be set below the cutoff, e.g. 45–50s instead of 600s), that every Redis `pubsub()` subscriber holds a dedicated TCP connection which pooling cannot share (so `maxclients` headroom and `pubsub.close()` strictly inside the generator's `finally` both matter), and that browsers cap ~6 HTTP/1.1 connections per origin (so h2/h3 negotiation should be confirmed during the ingress spike). Expects the resource budget to be stated explicitly, the numbers to be measured rather than assumed, and accepts belt-and-braces safety nets (a low-frequency fallback poll gated on tab visibility, re-auth before reconnect) over relying on one happy path. Confidence: 0.65

- Expects edits to stay diff-minimal: preserve a file's existing line-ending convention before rewriting it (this repo is mixed — CRLF in `app/core/config.py` and `app/main.py`, LF in the frontend files and the notification service) and confirm with `git diff` that only the intended new lines changed, so a small edit never lands as a whole-file rewrite. Reinforced on a JSX change: wrapping a block in a new permission condition initially re-indented the whole card (61-line diff for what should be a 2-line wrap), so the agent kept the original indentation untouched and the diff dropped to 9 lines — cosmetic re-indentation of untouched JSX is not worth the diff noise. Confidence: 0.6