# Taste
- Expects time-related code to use the project's shared/global timezone utility in `app.core.timezone` (e.g., `utcnow()`) rather than ad-hoc `datetime.now(timezone.utc)` — explicitly asked "did you use the global timezone we have in core for created at?", asked what benefit the central `core/timezone.py` provides, then asked whether it "should be used everywhere to ensure consistency," prompting a codebase-wide audit and migration of scattered `datetime.now(timezone.utc)` / `datetime.utcnow()` calls to the canonical `utcnow()`. Confidence: 0.85
- Prefers implementing features incrementally in phases rather than all at once — explicitly asked to "start with phase one only" and later "start phase 2" of a plan. Confidence: 0.9
- Treats plan documents in `docs/plans/` as the source of truth for implementation, referencing them directly when requesting work (e.g., "implement Bulk actions docs/plans/ux-suggestions.md"; "continue the next phase of docs\plans\portal-implementation-plan.md"). Confidence: 0.8
- Expects a detailed written plan to be created and saved as a file before implementation begins — "create a detailed plan and save it plans"; again asked to "create a full implementation plan" before the permanent Vercel migration. Planning artifacts are a prerequisite step, not optional. Confidence: 0.85
- Expects planning/implementation documents to include difficulty/effort assessment, risk analysis, a score (quantitative or qualitative rating), and suggestions for improvement where available — not just a list of tasks. Explicitly asked for "risk assisment and give it a score and suggestions to improve if avalible" alongside a detailed implementation plan. Confidence: 0.8
- Communicates in brief, terse directives (e.g., "start phase 2", "commit and push", "fix them", "i still do not see the notification. see the logs:") and delegates implementation details, exploration, verification, git operations, and even design judgment ("should we implement... if you see this is a good idea and a best practice, start implementing", "does this sounds good for the system? if yes implement it") to the agent. Confidence: 0.92
- Expects destructive or bulk actions (delete, clear all) to include an undo/redo safety net — explicitly asked for "a redo feat" alongside "clear all notifications", treating undo as a required companion to any irreversible action rather than a nice-to-have. Confidence: 0.75
- Expects bulk-delete/clear-all operations to be semantically aware — items that require explicit user resolution (e.g., actionable notifications like `amendment_pending`, `unlock_requested`) should survive bulk deletion; only dismissible/non-actionable items should be cleared. Reported as a bug when "delete all" wiped actionable notifications, and again when the UI briefly hid actionable items only for them to reappear after the undo timer — the UI must not show a misleading state during the undo window. Confidence: 0.85
- Reports bugs by pasting raw logs/tracebacks verbatim and expects the agent to diagnose the root cause from the evidence rather than being told the symptom — pasted a raw pytest short-test-summary (FAILED/ERROR blocks) with no other context and expected the agent to infer the empty-REDIS_URL root cause from the traceback and fix+push; again pasted an entire CI Docker build log with just "what is the error:" and expected the agent to pinpoint the failing `COPY /app/public` step from the log alone; and a third time pasted a bare compose/CI failure line (`network lims-internal declared as external, but could not be found`) with no instruction text at all, expecting the agent to root-cause the external-network mismatch across compose files; a fourth time pasted a raw container-startup log (`could not find a network matching network mode ilms-ierp_lims-internal`) with just "what is the issue:" and expected the agent to trace the stale-network-reference-after-rename root cause across the deploy/network state; a fifth time pasted a raw compose deploy log ending in `container portal_backend is unhealthy / dependency failed to start` with no accompanying text at all, expecting the agent to diagnose the unhealthy-container root cause (healthcheck vs. config vs. schema) from the log alone; a sixth time pasted a raw browser error verbatim (`This site can't be reached ... portal.aldrasat.edu took too long to respond ... ERR_CONNECTION_TIMED_OUT`) with just "can you check", expecting the agent to isolate whether the fault was network reachability (ports/security group/hosts/DNS) or server state (Caddy listening) by probing both ends itself; and a seventh time pasted the full AWS EC2 instance summary verbatim (instance state, EIP, private/public IPs, subnet/VPC IDs) with no commentary when asked what the instance showed, expecting the agent to parse the raw console output itself and continue the diagnosis; an eighth time pasted a raw Vercel build log (branch/commit hash, npm install output, deprecation warnings) with no accompanying text at all, expecting the agent to parse the deployment context itself and continue the ongoing EIP/backend diagnosis; and a ninth time pasted a raw Vercel build error verbatim (`No Output Directory named "public" found after the Build completed...`) prefixed only with "i find this error:", expecting the agent to diagnose the project-configuration root cause (wrong framework preset / output-directory setting) from the error text alone; and a tenth time pasted the raw Vercel middleware-crash page verbatim (`This Routing Middleware has crashed ... 500: INTERNAL_SERVER_ERROR Code: MIDDLEWARE_INVOCATION_FAILED ... https://ilms-ierp.vercel.app/`) alongside a design-behavior question, expecting the agent to root-cause the Edge-runtime `atob` bug and explain whether the portal's parents/students-only login was intended. Confidence: 0.98
- Treats deviations from the plan as issues to fix immediately rather than acceptable workarounds — when a "Note:" flagged that an implementation skipped a planned constraint, asked "does this note matter? ... if yes fix it now" and expected the planned behavior to be implemented. Confidence: 0.7
- Expects user-facing UI text to render as human-readable localized content, never raw i18n keys — complained that a notification displayed "notif.amendment_pending" verbatim ("which is not clear") and expected the translated message to appear instead. Confidence: 0.7
- Expects notifications to be actionable: when a notification appears, the user wants a clear path to act on it (e.g., accept/reject the amendment), not just a generic landing page — complained "i do not know how to accept it?". Confidence: 0.65
- Verifies the agent's claims against the actual codebase and calls out incorrect assertions (e.g., "you said there is not section id but what is this ...sections/[sectionId]?? or i got it wrong?"; "i am sure that we have implemented quided-confirmation, can you make sure again") — expects the agent to confirm code existence with accurate search patterns (matching real param names) and to exhaust alternative strategies (git log, direct file reads, commit history) when initial tool output produces false negatives, rather than trusting a single grep result. Confidence: 0.8
- Prefers consistent route/URL param naming across the codebase — suggested renaming the route param `[sectionId]` to `[id]` "for consistency". Confidence: 0.6
- Cautious about refactors that could break the codebase ("i am afraid we break the codebase") — expects the agent to verify impact (search all references, run the type check) before and after making naming/structural changes so nothing breaks. Also prompts to check every consumer of the changed code across the whole stack, including backend (e.g., asked "what about inside the emitter?" after a frontend route param rename) — not just the directly-edited files. Confidence: 0.7
- Expects derived/generated artifacts (HTML, PDF) to be regenerated after their source markdown files are modified — after a lengthy edit to `user-testing-guide.md`, prompted "also update the html guid" expecting the build script to be run, and in a separate instance explicitly asked to "update the user testing guid md and html to include this work". Confidence: 0.8
- Expects consistent UX patterns across related notification types — after seeing inline approve/reject buttons on `amendment_pending` notifications, asked to implement the same pattern for `unlock_requested` notifications (["should we implement the same thing for Day Unlock Request notifications... start implementing"]), treating the first implementation as a template to replicate. Confidence: 0.75
- Expects the agent to discover and use existing project scripts/tooling instead of inventing ad-hoc workflows — when the agent manually converted MD→HTML with `marked` CLI and PowerShell for several turns, asked "why don't you just use scripts\\build_testing_guide.py". Confidence: 0.85
- Expects individual-item actions (delete, dismiss) alongside bulk operations (clear all) — when a list supports a mass action, the user also wants per-item granular controls, not one or the other. Confidence: 0.7
- Expects mini/preview UI components (dropdown, popover) to provide navigation to their full-page counterparts — the notification bell dropdown should link to the full notifications page; teaser views are not dead-ends. Confidence: 0.6
- Expects post-action state feedback: after taking an action on an item (approve/reject/unlock), the item should remain visible with a status badge showing the resolution outcome rather than disappearing — the user wants to see what happened, not have the item vanish. Confidence: 0.75
- Expects resolved actionable items to become dismissible: items protected from bulk deletion (e.g., `amendment_pending`, `unlock_requested`) should be individually deletable once the user has taken action on them, so they don't linger permanently. Confidence: 0.7
- Expects data-altering user actions (approve, reject, dismiss) to persist their effects server-side so they survive a page refresh — validates by refreshing after taking action and treats ephemeral in-memory-only state as a bug. This extends to UI decorations derived from action outcomes (badges, status indicators): they must be computed from persisted state, not solely from transient client memory, or they will disappear on refresh. Confidence: 0.85
- Expects confirmation modals to be contextually accurate: the title, message, and confirm button label must match the specific action being confirmed — a dismiss/reject action must not show an approval confirmation dialog and vice versa. Confidence: 0.6
- Expects feature parity between related UI views — when a feature (e.g., a refresh button) is requested for one view of data, it should be applied to all related views simultaneously (dropdown/popover and full page); the user explicitly names all views that should receive the feature rather than expecting the agent to infer scope. Confidence: 0.7
- Expects planning/UX-suggestion documents to be updated after feature implementation to reflect completion status (e.g., marking items with ✅, linking to implementation plans) — these are living status-trackers, not just pre-implementation blueprints. Confidence: 0.65
- Expects implemented plan documents to be moved from `docs/plans/` to `docs/archive/plans/` rather than deleted or left in the active directory — plans have a lifecycle: active → archived, and the archive preserves them for reference. Confidence: 0.7
- Expects the user testing guide to be updated and rebuilt after every full feature implementation: update `docs/guides/user-testing-guide.md` with new test scenarios for the feature, then run `scripts/build_testing_guide.py` to regenerate the HTML (and any other derived formats). This is a required post-implementation step, not optional. Confidence: 0.85
- Expects new pages/modules to be registered in the roles/permissions system as toggleable permission entries. When pages are added, the user proactively asks whether they were added to the roles permissions page (e.g., "we have new pages: السجلات المالية، التقارير، الإشعارات، المردودات (Cashier)، الرواتب but were they been added to the roles permissions page?"), treating permission registration as a required companion step — not something to be caught later. If the frontend already references permissions (PAGE_PERMISSION_MAP, ROUTE_PERMISSION_MAP, sidebar), those permissions must exist in the DB with appropriate role grants — including core routes like `page_dashboard` for every role (e.g., `marketing_manager` must not get immediate Access Denied on `/[locale]/dashboard` and should land on a role-specific dashboard view, as seen when marketer login was blocked on dashboard). Confidence: 0.85
- Prefers to resume interrupted operations rather than abort and restart — when a transient connection error broke the session and the system prompted "Type 'continue' to try again," the user typed "continue" to retry the in-flight work instead of abandoning or restarting the task; expects interrupted flows to be resumed, not re-initiated. Confidence: 0.65
- Expects attendance and gradebook/final grades to be read-only for all roles except teacher — only the teacher role may create/modify attendance records and enter final grades/marks; all other users (including superadmin/manager/secretary) have view-only access. Confidence: 0.88
- Expects agent output (technical documents, plans, communication) to be in English, even though the project's end-user UI supports Arabic. Explicitly instructed "the output must be in english" when a plan was being drafted. Confidence: 0.65
- Prefers tables on mobile to support horizontal swipe/scroll to reveal hidden columns rather than being clipped with no scroll. Described issue as "i cannot see the full table lists i cannot move it to the side see the other columns" — expects to be able to swipe sideways to see all columns. Re-confirmed when calling out missed pages ("you still did not fix the users tables in users page and certificates page") until every table is swipeable. Confidence: 0.85
- Expects pattern-based UI fixes to be applied exhaustively across the entire codebase — will verify per-page and call out specific missed instances if the fix is incomplete. Prefers a systematic sweep for all occurrences (e.g., grep/walk for every `data-table` / `card overflow-hidden`) over patching only an initial batch of files. Confidence: 0.8
- Pushes back on plans that mark shared utilities as optional/low-priority — expects DRY extraction immediately even for ~15 lines, to avoid duplication drift where a bug is fixed in one copy and forgotten in the other. Confidence: 0.85
- Expects locale-aware formatting to reuse existing project i18n conventions (e.g., `frontend/lib/dates.ts`, `toLocaleTimeString`/`Intl.DateTimeFormat` with `ar-SA`/`en-US`) rather than hand-rolling hardcoded `AM`/`PM` strings, especially in `[locale]` (Arabic/English) routes — explicitly asks to check how `SectionsTable.tsx` already formats time and reuse it. Confidence: 0.88
- Expects disambiguation labels to be validated against the data model before assuming sufficiency — e.g., checks whether day-of-week is needed because two sections could share a time slot but run on different days, and wants schema confirmed before assuming time+room is enough. Confidence: 0.75
- Prefers explicit labeled suffixes in composite display strings — e.g., `Room 301` rather than bare `301` in `9:00 AM · 301` — so joined parts remain unambiguous to the user. Confidence: 0.72
- Provides detailed code-review pushback before merging and expects all points investigated before the plan is rewritten — including checking existing formatters, verifying schema, and addressing duplication, localization, and labeling explicitly. Confidence: 0.8
- Prefers student performance reporting to be per-section and printable directly from the student detail/profile page — covering attendance and grades (plus payments when relevant) to support parent inquiries — and expects the design to reserve a forward-compatible slot for AI-generated narrative/insights even when AI is out of scope for v1. Confidence: 0.75
- Expects the public landing page to represent the institute itself (programs, campus, diplomas, contact) — not the ERP system — with the ERP explicitly framed as an internal staff-only system; the public site is marketing/informational only. Confidence: 0.9
- Prefers staff access from the public landing to be a button/link redirecting to the dedicated login page (`/{locale}/login`) rather than an embedded login form on the landing, with no self-registration on the public site. Confidence: 0.9
- Prefers a single staff-login entry point in the header on the public landing and considers additional staff-login cards/sections redundant — explicitly asked to delete the duplicate "دخول الكادر فقط / نظام المعهد داخلي" cards because "the button in header is enough". Confidence: 0.82
- Prefers to evaluate alternate designs via isolated preview endpoints/routes (e.g., `/landing-v2`) with wholly distinct styling for side-by-side comparison before deciding what stays, rather than overwriting the existing page in place. Confidence: 0.8
- Rejects alternate design variants that reuse the same layout/structure even with different styling — expects each preview variant to have a fundamentally different layout, style, and look to enable meaningful comparison. Confidence: 0.85
- When exploring UIs with external web designers, prefers design briefs/prompts that describe the desired landing page in non-technical, product-focused terms (purpose, audience, content, tone and sections) without technical implementation details — exploring pure UI/visual options. Confidence: 0.8
- Wants AI positioned as a core, live pillar of the institute in all public-facing landing pages and designer briefs even before implementation is complete — explicitly asked to add AI as a main track alongside Languages/Computing and present AI-assisted features as already available rather than future/coming soon, treating AI-forward branding as a key differentiator. Confidence: 0.9
- After comparing design variants via isolated preview routes, expects the winning variant to be promoted to the canonical main route (`/[locale]/page.tsx`) and unused variants deleted — keeps a single source of truth rather than retaining multiple landing versions after a decision ("make it the only main one and delete others"). Confidence: 0.78
- Prefers collections like program tracks to use general, future-proof copy and scalable layouts rather than fixed-number framing — explicitly rejected "Three tracks. Same discipline." and its boxed 3-card grid because "we might have more," favoring an open-ended title/subtitle and a flexible row/list system that grows naturally as tracks are added. Confidence: 0.85
- Rejects generic AI-generated boilerplate taglines/pills on the landing (e.g., "معهد الدراسات · حلب — لغات وعلوم حاسب وذكاء اصطناعي") — explicitly asked to delete the pill because "i see it in every ai generated websites," prefers authentic, specific copy over cliché AI-site tropes. Confidence: 0.88
- Prefers logos rendered cleanly without visual artifacts — explicitly complained about "a square inside a circald" and expects a white card with border/padding and `object-contain` rather than dark square-in-circle or `mix-blend-screen` distortion. Confidence: 0.85
- Expects institute location to be consistently Taiz, Yemen (not Aleppo/حلب) across all surfaces — header, hero, tape, contact, footer, and both AR/EN locales — explicitly corrected the address from Aleppo to Taiz. Confidence: 0.92
- Prefers institute branding to be locale-aware and bilingual — header lockup shows Arabic "معهد الدراسات" when locale is Arabic rather than English-only "AL-DRASAT", with localized subtitle. Confidence: 0.78
- Expects complete bilingual coverage on the landing page, including decorative/animated elements like the moving ticker/banner — explicitly requested the ticker "should also support arabic" so both word list (Taiz/Languages/Coding/...) and framing text (— TAIZ — 1998 → NOW — → — تعز — ١٩٩٨ → الآن —) switch to Arabic when locale is Arabic, including Eastern Arabic numerals. Confidence: 0.82
- Prefers decorative elements like the moving ticker/banner to be constrained within the same body container as page content (`max-w-[1280px] px-4 md:px-6`, rounded-2xl card) rather than full-bleed edge-to-edge — explicitly asked that the moving banner "should be not start and end from the full screen let it the same body". Confidence: 0.78
- Expects moving ticker/marquee labels to loop seamlessly and infinitely without gaps or visible resets — explicitly asked to "fix the moving lable and make it infinite loop" rather than a scroll that gaps or resets. Confidence: 0.82
- Grants full creative autonomy for visual/UI polish — uses open-ended prompts like "try anything" when asking to improve a section's look (e.g., PROGRAMS section in `frontend/app/[locale]/page.tsx`) and expects the agent to autonomously explore and implement a high-end redesign without needing detailed specs or constraints. Confidence: 0.75
- Prefers clean stroke-style SVG line icons over placeholder text/emoji glyphs for program/feature cards — explicitly disliked rough glyphs like "Aa", "</>", "✦", "◧" in the PROGRAMS bento cards and expected replacement with crafted line icons (globe, code chevrons, sparkles, certificate). Confidence: 0.78
- Prefers success/done confirmation states (e.g., booking submission) to be polished with improved copy and animation rather than plain static text like "we received your book" — explicitly asked to "improve the done, we recevied your book thing to something better you might add some animation", expecting delightful animated feedback for completion states. Confidence: 0.75
- Expects primary institute branding/name ("AL-DRASAT" / "الدراسات") to remain visible on mobile viewports in both English and Arabic, while secondary tagline/location details may be hidden on small screens to save space — explicitly asked to "make the word Aldrasat visable in en and ar" when the header name was hidden via `hidden sm:block` on mobile. Confidence: 0.8
- Prefers strict separation between internal ERP and external student/parent portal — explicitly wants portal as separate deployable/container rather than extending ERP routes, to keep internal system simple and avoid coupling/bloat from public high-traffic features. Confidence: 0.9
- Expects external portal to be independently scalable and isolated for AI-heavy workloads — wants portal/AI on separate containers/services so load spikes and AI compute do not degrade ERP performance or stability. Confidence: 0.88
- When presented with a batch of related improvement suggestions, prefers to approve and implement all at once rather than selecting a subset — explicitly said "implement them all" for 5 landing-page upgrades, delegating full autonomous execution. Confidence: 0.75
- Prefers architecture/system documentation to include visual diagrams (Mermaid flowcharts, sequence diagrams) alongside text — explicitly requested portal design be written "in docs with diagrams". Confidence: 0.68
- Expects AI architecture to cover both current internal AI (question generation pipeline) and future student-facing AI features in a unified but workload-isolated plane — explicitly questioned whether AI isolation meant "only" internal question generation or "all AI features including ... for students" and whether future AI was taken into account, indicating future-proof AI scope is required. Confidence: 0.75
- Wants a single general login button/page for all user types (staff, students, parents) rather than separate role-specific login entries — confirmed "will you change the staff login button to be a general login button? we need it general," with one shared login URL and automatic role detection that redirects staff to the ERP dashboard and students/parents to the portal. Re-confirmed when the user saw the portal's own parents/students-only login page and asked "while we have implemented a unified login. how is this working?" — expecting any divergent login surface to be explained as an intentional fallback consistent with the unified-login plan. Confidence: 0.75
- When the user supplies an exact command for remote access (e.g., `ssh -i <key> ubuntu@ec2-...compute.amazonaws.com`), expects the agent to use it as given rather than persisting with its own approach — the DNS hostname form succeeded after repeated raw-IP attempts timed out, showing the user-provided connection string is authoritative over the agent's inferred alternatives. Confidence: 0.6
- Expects the agent to respect the established SSH key file path (`E:\lms\SSH-KEY.pem`) and working connection route (public DNS hostname via IPv6/NAT64 when the bare EIP IPv4 is unroutable from the agent's network) rather than inventing new paths — repeated SSH probes used the exact key and either the bare EIP or the canonical `ec2-<ip>.eu-north-1.compute.amazonaws.com` hostname. Confidence: 0.62
- Prefers the agent to gain direct access (SSH credentials) to remote infrastructure to diagnose issues itself rather than being handed commands to run — after receiving log-extraction commands to run on EC2, asked "what credentials do you need to ssh by urself?", and when asked for credentials, pointed the agent to repo-stored secrets (host/username in `.env`, key file in the repo root) expecting the agent to self-discover and use them without further hand-holding — signaled "YOU WILL FIND THE USERNAME AND HOST IN THE .ENV FILE, AND THE KEY IS NAMED SSH-KEY IN THE ROOT DIR". Expects the agent to locate repo-stored infra credentials itself rather than asking the user to paste them — again pointed to the repo ("key is in the root dir and username is in the .env file") when the website was down, expecting the agent to find the SSH key and host itself. Confidence: 0.8
- Prefers Dockerfiles copied/templated from a sibling project to be checked against the new project's actual layout before use — a `COPY --from=builder /app/public ./public` inherited from the main frontend's Dockerfile broke the portal image build because the portal has no `public/` dir; expects container builds to succeed even when optional dirs are absent (conditional copy `test -d ... && cp ... || true`), since `next build` succeeded and only the copy step failed. Confidence: 0.7
- Expects deploy/setup scripts to be robust against stale infrastructure state left over from previous deployments — when renamed network left containers referencing the old project-scoped network name, expected the deploy script to `up -d --force-recreate` (instead of plain `up -d`) so existing containers are rebuilt with current network config automatically on every deploy rather than requiring a manual `docker compose down`. Confidence: 0.7
- Expects environment-config helpers in setup scripts to be idempotent across server `.env` files of any age — when `set_env`'s `sed s|^KEY=.*|` silently did nothing for keys absent from a pre-existing `.env` (leaving `PORTAL_JWT_SECRET`/`ERP_SERVICE_KEY` unset and crashing the portal backend at startup), expected the helper to append missing keys rather than only replace existing ones, so secrets added later are always written on old servers. Confidence: 0.75
- Expects landing/content changes to be immediately reflected in the marketing_manager CMS — when new public landing blocks/fields are added (e.g., social proof stats/testimonials, program outcome/duration/intake/seats/priceHint, FAQ, WhatsApp phone), the marketing content editor (`app/[locale]/(dashboard)/dashboard/content/page.tsx` backed by JSONB `landing_content.value`) must be updated in the same change so the marketing role can edit them; proactively verifies by asking "did you update the marketing user account to include the new things?" after landing upgrades. Confidence: 0.85
- Expects zero flash/flicker of default fallback content before CMS content on refresh — reported seeing default landing content for seconds before CMS content appeared and demanded fix; expects cache-first rendering (localStorage `landing:ar/en`) or skeleton loader while fetching so CMS content appears instantly without visible default-then-swap, and expects CMS save to immediately update the frontend cache so edits are reflected instantly. Confidence: 0.85
- Expects new implementation plans to be saved in `docs/plans/` and related existing plan files (e.g., `docs/plans/ai-pipeline-implementation-plan.md`) to be reviewed and updated if necessary for consistency when scope overlaps — explicitly asked to "create a full implementation plan and save it in docs/plans and update docs\plans\ai-pipeline-implementation-plan.md if nessary". Confidence: 0.75
- Prefers dashboard sidebar navigation ordered by logical workflow grouping (Overview → Academics → Students & Learning → Finance & Commerce → Team & Access → Engagement & Content → Insights & Administration) rather than scattered or alphabetical flat list — explicitly asked to reorder sidebar items to "most logical order" delegating grouping judgment to the agent. Confidence: 0.68
- Expects local Docker environment to stay in sync with production and flags stale containers as a bug — noticed database backups page still showed 'under development' locally while production already had the feature, and expects `docker compose build` + `docker compose up -d` (or `--force-recreate`) to actually recreate frontend/backend containers so the latest images run, not just built, with a clear explanation of container-vs-image age drift. Repeated same report for /ar/dashboard/settings after frontend was verified current, reinforcing expectation that local reflects production immediately after rebuild. Confidence: 0.84
- Prefers to test the running system hands-on after implementation and expects the agent to own the local container lifecycle — explicitly asked to "run all containers locally so i can test" after deleting them all, expecting a full build-from-scratch bring-up of the entire stack rather than relying on automated tests alone. Confidence: 0.6
- Explicitly scopes informational questions with "question only answer here", "question only here answer", "fast answer here", "answer here only and fast", or "only answer here fast" — expects a direct, concise factual answer (backed by codebase verification) without the agent starting implementation or making changes; a pure Q&A mode distinct from implementation requests. Confidence: 0.92
- Prefers a single root-domain URL scheme with locale-prefixed paths for public entry points — landing page at the domain root (`aldirasat.com`) and login at `aldirasat.com/{ar|en}/login` — and expects the architecture/config (CORS origins, SSO flow) to be designed around the real production domain even while temporarily running on an EC2 public IP. Confidence: 0.7
- Prefers the external portal to live on a separate subdomain (e.g., `portal.aldirasat.com`) rather than under a path on the main domain — explicitly chose "subdomain" over `aldirasat.com/portal` because a real domain will be bought later, keeping the two-app/SSO separation intact even during EC2 public-IP development. Confidence: 0.7
- Expects the agent to diagnose remote accessibility issues by probing both ends of the network path itself — checks public reachability (curl to the public IP / redirect headers) AND server-side state (ss/docker ps for listening ports) before concluding where the failure lies; and expects infrastructure-exposing actions (opening AWS security-group port 443, pointing DNS) to be offered as options with explicit confirmation requested before execution, never done unilaterally. When the user asserts infrastructure state themselves (e.g., "i checked the instance and the ports 22 and 80 are open"), the user still expects empirical verification rather than accepting the console claim — raw TCP probes to the same IP showed SSH/22 OPEN while HTTP/80 TIMED OUT, proving a security-group/ACL blockage — and an explanation of the regression (why a previously-working access path like browsing via the public IP stopped working) covering server redeploys, IP reassignment, and changed source IPs. Confidence: 0.85
- Prefers internal services to remain private/internal-only and declines offers to expose them publicly — when asked whether to point DNS at the EC2 IP and open port 443 so the portal is publicly reachable, answered "no don't do it"; expects the agent to accept the decline and leave the portal inaccessible-from-outside rather than pushing toward making it public, until the user explicitly decides otherwise. Confidence: 0.7
- The agent's shell environment is Windows cmd.exe (paths like `portal\frontend`, `set VAR=...`, `if exist`, `findstr`), so bash-only constructs fail — a `git commit -F - <<'EOF'` heredoc did not work and had to be replaced by writing the commit message to a temp file and using `git commit -F <file>`; expects all shell commands to be cmd-compatible. Confidence: 0.7
- When proposing a hosting/architecture change framed as "what do you think?", expects a candid feasibility assessment from the agent — including the cross-origin gotchas of a frontend/backend split (HttpOnly cookies, SameSite, CORS, proxying `/api` via rewrites) and which config changes are required — before deciding whether to proceed. Confidence: 0.65
- Prefers pragmatic, low-friction hosting over complex self-managed infrastructure — when Cloudflare tunnel/DNS configuration felt hard ("this is hard"), proposed switching the frontend to Vercel free hosting to get a working public URL immediately, with the custom domain to be added to Vercel later once purchased; comfortable splitting the stack across platforms (frontend on Vercel, backend/db staying on EC2) rather than fighting DNS/tunnel complexity on one server; later committed to this as a *permanent* migration ("create a full implementation plan to permanently move to vercel"), choosing Vercel Git integration (zero CI changes, auto-deploy on push) over a manual CLI deploy step. Confidence: 0.85
- Prefers to stop/remove services that are superseded by a migration rather than keep them running as idle fallback — when asked what to do with the Docker frontends on EC2 after moving to Vercel, chose "Stop them" (remove from docker-compose and setup.sh once Vercel is live) over keeping them as a fallback, to free resources and keep one deployment platform. Confidence: 0.7
- When reviewing a proposed architecture change, asks pointed questions about whether existing components are still necessary or redundant (e.g., "do we still need caddy in the new setup or it is reduntent?") — expects the agent to justify each component's continued role by explaining exactly what would be lost by removing it (TLS on the Vercel→EC2 leg, proxying to unpublished backend ports, security headers) and to distinguish "role shrinks" from "fully redundant" rather than giving a bare yes/no. Confidence: 0.6
- Expects the agent to re-probe network reachability of critical infrastructure immediately when a parallel process (e.g., a Vercel build) is in flight — while the Vercel deployment built, expected IPv4 reachability to the EIP to be re-tested so the frontend's `/api/v1` rewrites were known-good (or known-broken) before the deployment finished; network re-verification is the critical dependency, not the build itself. Confidence: 0.55
- Prefers to rule out the user's own local network as the cause of reachability failures before changing infrastructure — explicitly offered "try accessing from a different network — e.g., your phone on cellular data (not Wi-Fi), or a VPN" as a faster test than re-allocating the EIP, so an ISP/local-network block is not mistaken for an AWS problem. Confidence: 0.5
- Expects the agent to reason about whether a third-party platform (Vercel) can reach the backend from a different network — the deployed Vercel frontend's health endpoint (`https://<erp>.vercel.app/api/v1/health`) is a real test of whether the EIP is globally routable vs. blocked only from the agent's own network; 200 means Vercel CAN reach the backend and the EIP is globally fine, 502 means the IPv4 path is globally broken. Confidence: 0.5
- Prefers to handle AWS console/account changes manually rather than have the agent attempt them — when approving the Vercel migration, said "if need to anything to do with aws just tell me and i wll do it manually", expecting the agent to implement everything reachable via SSH/repo itself and to surface AWS-only steps (e.g., opening security-group ports, since the CLI keys are expired) as explicit instructions the user performs in the AWS console. Confirmed again when the user allocated and associated the Elastic IP themselves in the console ("i have allocaated an elastic ip to the instance 13.50.176.4") and the agent again handed back the console-side diagnosis (EIP association check, security group, network ACL) for the user to run; now drives AWS-side work through the AWS MCP himself — asked for a detailed troubleshooting doc because "i will use aws mcp to solve this" — so the agent's role is to prepare handoff materials and implement everything reachable via SSH/repo, never to execute AWS changes. Confidence: 0.8
- Uses the AWS root account directly — stated "i do not have IAM users", confirmed root-account sign-in with no AWS Organization — so AWS guidance must assume root-level permissions: IAM-user/role-based fixes (e.g., attaching the `AWSMCPSignInOAuthAccessPolicy` to an IAM user) are not applicable, and root-account OAuth flows work without policy grants. Confidence: 0.6
- Prefers to unblock immediately with local patches to installed tooling rather than wait for upstream fixes — chose "Patch CLI now" (patching the command-code CLI's OAuth flow to send the `resource` parameter AWS MCP requires) over reporting upstream or switching auth architectures, even though the patch is overwritten on every CLI update and needs re-applying. Confidence: 0.55
- Wants discovered bugs/fixes in third-party tooling to be contributed back upstream — after locally patching the command-code CLI, asked "maybe we can use this issue to contribute to command-code repo in github, how can i do this", expecting a concrete contribution path (file an issue documenting the root cause, fork, fix in the source `src/` rather than the shipped `dist/`, verify, PR referencing the issue). Confidence: 0.65
- When filing GitHub issues (e.g., for an upstream contribution), expects the report to follow the standard structured bug-report template sections — Expected Behavior, Actual Behavior, Steps to reproduce the issue, and the tool/Command Code Version — explicitly listed exactly those four sections and asked for the issue body to include them; expects the agent to produce a ready-to-paste issue body in that format, including environment details (OS, Node version). Confidence: 0.75
